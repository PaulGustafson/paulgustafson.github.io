\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsfonts, amsmath, amssymb} % For mathematical symbols

\newtheorem{exercise}{Exercise}
\newcommand{\trace}{\operatorname{tr}}
\newcommand{\ev}{\operatorname{ev}}
\newcommand{\coev}{\operatorname{coev}}
\newcommand{\im}{\operatorname{im}}
\newcommand{\End}{\operatorname{End}}



\title{Coordinate-free Mathematics}
\author{Paul Gustafson}
\date{May 2025}

\begin{document}


\maketitle
The first section develops algebraic constructions independent of a basis; the second develops analytic constructions from norms, operators, and positivity; the third develops geometric constructions independent of coordinates.

\section{Algebra}

Linear algebra studies linear maps between vector spaces. A \textbf{vector space} $V$ over a field $k$ is a set equipped with vector addition and scalar multiplication operations satisfying straightforward compatibility conditions. A \textbf{linear map} $f: V \to U$ between $k$-vector spaces is a map of underlying sets such that $f(\alpha v + \beta w) = \alpha f(v) + \beta f(w)$ for all $\alpha, \beta \in k$ and $v, w \in V$. Examples include differential operators, group representations, coefficient matrices of linear systems of equations, and local approximations of globally nonlinear functions \cite{halmos,hoffman-kunze}.

The \textbf{free $k$-vector space on a set $X$}, denoted $kX$, is the set of finitely supported set-theoretical functions $X \to k$ with pointwise addition and scalar multiplication. The \textbf{direct sum} vector space $V \oplus W$ is the set $V \times W$ with pointwise addition and scalar multiplication defined by $\alpha (v \oplus w) = \alpha v \oplus \alpha w$. The \textbf{quotient} of $V$ by a subspace $W$, denoted $V / W$, is the set of equivalence classes of $V$ modulo the relation $x \sim y$ iff $x - y \in W$. The \textbf{tensor product} $V \otimes W$ is the vector space $k(V \times W) / N$ where $N$ is the subspace generated by the obvious linearity relations on $V$ and $W$.

An \textbf{isomorphism} of vector spaces $V\cong W$ consists of linear maps $f:V\to W$ and $g:W\to V$ such that $f\circ g=1_W$ and $g\circ f=1_V$.

\begin{exercise}[Universal constructions]\leavevmode\par
Prove the following:
\begin{itemize}
\item $k(X \sqcup Y) \cong kX \oplus kY$
\item $k(X \times Y) \cong kX \otimes kY$
\item The first isomorphism theorem for vector spaces (and second and third if you want).
\item Find universal properties determining up to canonical isomorphism the free $k$-vector space, direct sum, quotient, tensor product, and dual space (defined below), respectively.
\end{itemize}
\end{exercise}

The \textbf{dual space} $V^*$ is the space of linear maps from a vector space $V$ to its ground field $k$. The evaluation map $\ev_V : V^* \otimes V \to k$ is the linear extension of the evaluation morphism $f \otimes x \mapsto f(x)$. We say that $V$ is \textbf{finite dimensional} if there is a co-evaluation morphism $\coev_V: k \to V \otimes V^*$ satisfying the snake equations $(1_{V} \otimes \ev_V) \circ (\coev_V \otimes 1_V) = 1_V$
and
$(\ev_V \otimes 1_{V^*}) \circ (1_{V^*} \otimes \coev_{V}) = 1_{V^*}$. We define the \textbf{dimension} $\dim(V) = \trace(1_V)$, where $\trace(f) : \End(V) \to k$ is given by $f \mapsto (\ev_V \circ s_{V, V^*} \circ (f \otimes 1_{V^*}) \circ \coev_V)(1)$ where $s_{X, Y} : X \otimes Y \to Y \otimes X$ is the symmetric braiding $x \otimes y \mapsto y \otimes x$ \cite{ponto-shulman}.



\begin{exercise}[Trace and dimension identities]\leavevmode\par
Prove the following for finite-dimensional vector spaces $V,W$ and linear maps $f,g:V\to V$.
    \begin{itemize}
        \item $\trace(f g) = \trace(g f)$
        \item $\trace(f \oplus g) = \trace(f) + \trace(g)$
        \item $\trace(f \otimes g) = \trace(f) \trace(g)$
        \item $\dim(V) \in \mathbf{N}$
        \item $V \cong W$ iff $\dim(V) = \dim(W)$
    \end{itemize}
\end{exercise}


Define the tensor algebra and \textbf{exterior algebra of $V$} by \cite{lang}
\[
T(V)=\bigoplus_{k\geq 0}V^{\otimes k},
\qquad
\Lambda V=T(V)/\langle v\otimes v:v\in V\rangle.
\]
The space $\Lambda^kV$ is the degree-$k$ component of $\Lambda V$.

\begin{exercise}[Top exterior power]
    Show that the highest graded component of $\Lambda V$ is one-dimensional.
\end{exercise}

The \textbf{determinant} $\det(f)$ is the coefficient of the linear map on the highest-graded component of $\Lambda V$ given by $v_1 \wedge \cdots \wedge v_{\dim(V)} \mapsto f(v_1) \wedge \cdots f(v_{\dim(V)})$.

For an $n$-dimensional real vector space $V$, its \textbf{determinant line} is $\det(V^*)=\Lambda^nV^*$. An \textbf{orientation} is a choice of one of the two connected components of $\det(V^*)\setminus\{0\}$; the elements of the chosen component are positive.

\begin{exercise}[Determinant identities and volume]\leavevmode\par
Prove the following:
\begin{itemize}
    \item $\det(f \oplus g) = \det(f) \det(g)$
    \item If $V = \mathbb{R}^n$, then $\det(f)$ is the signed volume of $f(I^n)$ where $I = [0,1]$.
    \item Cramer's rule
\end{itemize}
\end{exercise}

For $f\in\End(V)$, the \textbf{characteristic polynomial} is the polynomial $\lambda \mapsto \det(f - \lambda 1_V)$.

A scalar $\lambda\in k$ is an \textbf{eigenvalue} of $f$ if $\ker(f-\lambda1_V)\neq0$. This kernel is its \textbf{eigenspace}; its dimension is the geometric multiplicity of $\lambda$.

\begin{exercise}[Characteristic polynomial and Jordan form]\leavevmode\par
    Let $f\in\End(V)$ with $V$ finite dimensional. Suppose its characteristic polynomial splits over $k$. Prove:
    \begin{itemize}

        \item Every eigenvalue of $f$ is a root of its characteristic polynomial.
        \item The geometric multiplicity of each eigenvalue is at most equal to its \textbf{algebraic multiplicity} (i.e. multiplicity as a root of the characteristic polynomial)
        \item $\trace(f) = \sum_i \lambda_i$ and $\det(f) = \prod_i \lambda_i$, where each eigenvalue $\lambda_i$ is repeated according to its algebraic multiplicity
        \item The operator $f$ satisfies its own characteristic polynomial (replacing $\lambda$ with $f$)
        \item The existence of the Jordan normal form for a linear operator on a finite-dimensional complex vector space
    \end{itemize}
\end{exercise}

\subsection{Group representations}

An action of a group $G$ on a set $X$ assigns to each $g\in G$ a bijection $x\mapsto gx$ such that $1x=x$ and $(gh)x=g(hx)$. A \textbf{representation} of $G$ on a vector space $V$ is a homomorphism
\[
\rho:G\longrightarrow\operatorname{GL}(V).
\]
A subspace $W\subseteq V$ is \textbf{$G$-invariant} if $\rho(g)W\subseteq W$ for every $g\in G$. Restriction gives the \textbf{subrepresentation} $W$, and $\rho(g)(v+W)=\rho(g)v+W$ gives the \textbf{quotient representation} $V/W$. A nonzero representation is \textbf{irreducible} if its only invariant subspaces are $0$ and $V$.

For representations $V$ and $W$, the direct sum, tensor product, and dual representations are defined by
\[
\begin{aligned}
g(v,w)&=(gv,gw),\\
g(v\otimes w)&=gv\otimes gw,\\
(g\phi)(v)&=\phi(g^{-1}v),\qquad \phi\in V^*.
\end{aligned}
\]
An \textbf{intertwiner} $T:V\to W$ is a linear map satisfying $T\rho_V(g)=\rho_W(g)T$ for every $g$. Write $\operatorname{Hom}_G(V,W)$ for the space of intertwiners and
\[
V^G=\{v\in V:\rho(g)v=v\text{ for every }g\in G\}
\]
for the fixed-point space \cite{serre-representations}.

\begin{exercise}[Invariant averaging]\leavevmode\par
\textit{Let $G$ be finite and let $V$ be a complex representation. Define}
\[
P_G=\frac1{|G|}\sum_{g\in G}\rho(g).
\]
\textit{Prove that $P_G$ is the projection of $V$ onto $V^G$. Prove also that averaging any Hermitian inner product over $G$ produces a $G$-invariant Hermitian inner product, so every finite-dimensional complex representation of $G$ is equivalent to a unitary representation.}
\end{exercise}

\subsection{Maschke's theorem and Schur's lemma}

\begin{exercise}[Maschke's theorem]\leavevmode\par
\textit{Let $W\subseteq V$ be $G$-invariant and let $P:V\to W$ be any linear projection. Define}
\[
\widetilde P
=\frac1{|G|}\sum_{g\in G}\rho(g)P\rho(g)^{-1}.
\]
\textit{Prove that $\widetilde P$ is $G$-equivariant and has image $W$. Deduce that $\ker\widetilde P$ is a $G$-invariant complement of $W$. If $G$ is finite and the characteristic of $k$ does not divide $|G|$, prove that every finite-dimensional $k$-representation of $G$ is a direct sum of irreducible representations.}
\end{exercise}

\begin{exercise}[Schur's lemma]\leavevmode\par
\textit{Let $V$ and $W$ be irreducible finite-dimensional complex representations. Prove that every nonzero intertwiner $T:V\to W$ is an isomorphism and that}
\[
\End_G(V)=\mathbb C1_V.
\]
\textit{Let $\mathbb C[G]$ act on $V$ by the linear extension of $\rho$. Deduce that every element of the center $Z(\mathbb C[G])$ acts by a scalar on each irreducible representation.}
\end{exercise}

\subsection{Characters and orthogonality}

The \textbf{character} of a finite-dimensional representation $V$ is the function
\[
\chi_V(g)=\trace(\rho_V(g)).
\]
For class functions $\chi,\psi:G\to\mathbb C$, define
\[
\langle\chi,\psi\rangle_G
=\frac1{|G|}\sum_{g\in G}\overline{\chi(g)}\psi(g).
\]

\begin{exercise}[Great Orthogonality Theorem]\leavevmode\par
\textit{Prove that characters are class functions and that, for unitary representations,}
\[
\chi_{V\oplus W}=\chi_V+\chi_W,
\qquad
\chi_{V\otimes W}=\chi_V\chi_W,
\qquad
\chi_{V^*}(g)=\overline{\chi_V(g)}.
\]
\textit{Let $\Gamma^\alpha$ and $\Gamma^\beta$ be irreducible unitary representations of a finite group $G$, of dimensions $d_\alpha$ and $d_\beta$. In orthonormal bases, prove that}
\[
\sum_{g\in G}
\Gamma^\alpha(g)_{ij}
\overline{\Gamma^\beta(g)_{k\ell}}
=\frac{|G|}{d_\alpha}
\delta_{\alpha\beta}\delta_{ik}\delta_{j\ell}.
\]
\textit{Deduce that their characters satisfy}
\[
\langle\chi_\alpha,\chi_\beta\rangle_G
=\delta_{\alpha\beta}.
\]
\textit{If $V$ is a finite-dimensional representation, prove that the multiplicity of $V_\alpha$ in $V$ is}
\[
m_\alpha=\langle\chi_\alpha,\chi_V\rangle_G.
\]
\textit{Conclude that the character determines a finite-dimensional complex representation up to isomorphism.}
\end{exercise}

For an irreducible character $\chi_\alpha$, define the \textbf{symmetry projection}
\[
P_\alpha
=\frac{d_\alpha}{|G|}
\sum_{g\in G}\overline{\chi_\alpha(g)}\rho(g).
\]

\begin{exercise}[Symmetry projections]\leavevmode\par
\textit{Prove that the operators $P_\alpha$ are pairwise orthogonal idempotents and that $P_\alpha V$ is the $\alpha$-isotypic component of $V$. Interpret $P_\alpha$ as constructing symmetry-adapted linear combinations of atomic orbitals or displacement vectors.}
\end{exercise}

\subsection{Molecular symmetry}

The \textbf{point group} of a molecule is the finite group $G\subseteq O(3)$ of spatial symmetries preserving its nuclear configuration, including atomic species. For a molecule with $N$ atoms, $G$ acts on the real displacement space
\[
V_{\mathrm{disp}}=\bigoplus_{a=1}^N\mathbb R^3_a
\cong\mathbb R^{3N}
\]
by permuting atoms and applying the spatial linear action to each displacement. The three-dimensional translational representation carries the polar-vector action $R_g$, while the rotational representation carries the axial-vector action $\det(R_g)R_g$; denote them by $V_{\mathrm{trans}}$ and $V_{\mathrm{rot}}$. We complexify these real representations when applying character theory. For a nonlinear molecule, the vibrational representation is the class
\[
[V_{\mathrm{vib}}]
=[V_{\mathrm{disp}}]-[V_{\mathrm{trans}}]-[V_{\mathrm{rot}}]
\]
in the representation ring \cite{cotton-chemical}.

\begin{exercise}[Character of molecular displacements]\leavevmode\par
\textit{For $g\in G$, prove that}
\[
\chi_{\mathrm{disp}}(g)
=\sum_{\substack{a\\g(a)=a}}\trace(R_g),
\]
\textit{where $R_g:\mathbb R^3\to\mathbb R^3$ is the spatial action and the sum is over atoms fixed by $g$. Prove that $\dim V_{\mathrm{vib}}=3N-6$ for a nonlinear molecule, and use character orthogonality to express the multiplicity of every irreducible representation in $V_{\mathrm{vib}}$.}
\end{exercise}

For water, take the $z$-axis along the $C_2$ axis and the molecule in the $yz$-plane. Its point group is
\[
C_{2v}=\{E,C_2,\sigma_{xz},\sigma_{yz}\}.
\]
\begin{exercise}[Vibrations of $H_2O$]\leavevmode\par
\textit{Use the group law and character orthogonality to derive the character table}
\[
\begin{array}{c|rrrr|c}
 &E&C_2&\sigma_{xz}&\sigma_{yz}&\text{basis functions}\\ \hline
A_1&1&1&1&1&z\\
A_2&1&1&-1&-1&R_z\\
B_1&1&-1&1&-1&x,\ R_y\\
B_2&1&-1&-1&1&y,\ R_x.
\end{array}
\]
\textit{Compute}
\[
\chi_{\mathrm{disp}}=(9,-1,1,3),
\qquad
\chi_{\mathrm{trans}}=(3,-1,1,1),
\qquad
\chi_{\mathrm{rot}}=(3,-1,-1,-1).
\]
\textit{Use character orthogonality to prove that}
\[
V_{\mathrm{vib}}\cong2A_1\oplus B_2.
\]
\textit{Identify the two $A_1$ modes as the symmetric stretch and bend, and the $B_2$ mode as the asymmetric stretch. Construct symmetry-adapted displacement vectors using the projections $P_{A_1}$ and $P_{B_2}$.}
\end{exercise}

\subsection{Spectroscopic selection rules}

Let $V_i$ and $V_f$ be the symmetry types of initial and final states. The electric-dipole operator transforms as the vector representation $V_\mu=V_{\mathrm{trans}}$. A transition amplitude
\[
\langle\psi_f,\mu\psi_i\rangle
\]
is the matrix coefficient used below.

\begin{exercise}[Electric-dipole selection rule]\leavevmode\par
\textit{Use the Great Orthogonality Theorem to prove that the transition amplitude vanishes unless}
\[
1\subseteq V_f^*\otimes V_\mu\otimes V_i.
\]
\textit{For irreducible $V_f$, prove that this is equivalent to}
\[
V_f\subseteq V_\mu\otimes V_i.
\]
\textit{The vibrational ground state of water has type $A_1$. Since}
\[
V_\mu\cong A_1\oplus B_1\oplus B_2,
\]
\textit{deduce that both $A_1$ modes and the $B_2$ mode are infrared active. Thus molecular symmetry alone predicts that all three fundamental vibrations of water are allowed in infrared spectroscopy.}
\end{exercise}

\begin{exercise}[Optional Raman selection rule]\leavevmode\par
\textit{Let the polarizability tensor carry $\operatorname{Sym}^2(V_\mu)$. Prove that a vibrational mode is Raman active precisely when its irreducible representation occurs in this symmetric square. Prove that}
\[
\operatorname{Sym}^2(V_\mu)
\cong3A_1\oplus A_2\oplus B_1\oplus B_2
\]
\textit{for $C_{2v}$. Deduce that all three fundamental vibrational modes of water are Raman active.}
\end{exercise}

Finite-group representations turn molecular symmetry into experimentally testable selection rules. The monoidal categories below retain irreducible decomposition and tensor product as fusion rules, and add associators and braiding.

\subsection{Monoidal and braided categories}

A $\mathbb C$-linear category is a category whose morphism sets
\[
\operatorname{Hom}_{\mathcal C}(x,y)
\]
are complex vector spaces and whose composition is bilinear.

A $\mathbb C$-linear monoidal category is a $\mathbb C$-linear category equipped with a tensor product $\otimes$ that is bilinear on morphisms and is associative with tensor unit $1$, up to coherent natural isomorphism. Direct sums and duals generalize the corresponding vector-space constructions.

Let $\mathcal C$ be such a category with finite direct sums. An object $x$ is \textbf{simple} if $\End(x)\cong\mathbb C$, and $\mathcal C$ is \textbf{semisimple} if every object is a finite direct sum of simple objects. Assume that $1$ is simple, that there are finitely many isomorphism classes of simple objects, and that $\mathcal C$ is \textbf{rigid}: every $x$ has a dual $x^*$ with evaluation and coevaluation maps satisfying the snake identities \cite{etingof-tensor}.

The definitions of duality, trace, and dimension above extend verbatim to $\mathcal C$. Write $d_x=\trace(1_x)$.
Assume that the categorical dimensions of nonzero objects are positive real numbers.

\begin{exercise}[Categorical trace and dimension]\leavevmode\par
\textit{Prove that categorical trace is cyclic and that}
\[
d_{x\oplus y}=d_x+d_y,
\qquad
d_{x\otimes y}=d_xd_y.
\]
\end{exercise}

For $x,y,z\in\mathcal C$, write
\[
F^{xyz}:(x\otimes y)\otimes z\longrightarrow x\otimes(y\otimes z)
\]
for the associator. A \textbf{braiding} is a family of isomorphisms
\[
c_{x,y}:x\otimes y\longrightarrow y\otimes x.
\]
For $f:x\to x'$ and $g:y\to y'$, it satisfies $(g\otimes f)c_{x,y}=c_{x',y'}(f\otimes g)$.
The associator satisfies the pentagon identity, and the associator and braiding satisfy the hexagon identities. Suppressing associators, the latter are
\[
\begin{aligned}
c_{x,y\otimes z}&=(1_y\otimes c_{x,z})(c_{x,y}\otimes1_z),\\
c_{x\otimes y,z}&=(c_{x,z}\otimes1_y)(1_x\otimes c_{y,z}).
\end{aligned}
\]

The \textbf{monodromy} of $x$ and $y$ is the double braiding
\[
M_{x,y}=c_{y,x}c_{x,y}:x\otimes y\longrightarrow x\otimes y.
\]
Thus $c_{x,y}$ is one crossing, while $M_{x,y}$ is a full braid. If $x\otimes y$ is simple, identify $M_{x,y}$ with its scalar in $\mathbb C^\times=\mathbb C\setminus\{0\}$. Decompositions of tensor products of simple objects are \textbf{fusion rules}, and the spaces $\operatorname{Hom}_{\mathcal C}(z,x\otimes y)$ are \textbf{fusion spaces}.

An object $x$ is \textbf{invertible} if $x\otimes x^*\cong1$. Assume first that every simple object is invertible and that their isomorphism classes form the cyclic group
\[
\{1,a,a^2,\ldots,a^{m-1}\},
\qquad
a^r\otimes a^s\cong a^{r+s\pmod m}.
\]
Write $U(1)=\{z\in\mathbb C:|z|=1\}$ and $M_{r,s}=M_{a^r,a^s}\in U(1)$.

\begin{exercise}[Abelian braiding phase]\leavevmode\par
\textit{Use the hexagon identities to prove that}
\[
M_{r+s,t}=M_{r,t}M_{s,t},
\qquad
M_{r,s+t}=M_{r,s}M_{r,t}.
\]
\textit{Deduce that there is an integer $k$ modulo $m$ such that}
\[
M_{r,s}=\exp\left(\frac{2\pi i krs}{m}\right).
\]
\textit{For $k=1$, prove that the monodromy of $a$ about $N$ copies of $a$ is multiplication by}
\[
\exp\left(\frac{2\pi iN}{m}\right).
\]
\textit{In particular, for $m=3$, increasing $N$ by one changes the phase by $2\pi/3$.}
\end{exercise}

The phase $2\pi/3$ is the value observed by interferometry in the $\nu=1/3$ fractional quantum Hall state \cite{nakamura-anyons}.

Now suppose that $\mathcal C$ has two simple objects $1,\tau$ satisfying the fusion rule
\[
\tau\otimes\tau\cong1\oplus\tau.
\]

\begin{exercise}[Fibonacci categorical dimension]\leavevmode\par
\textit{Let $d_\tau=\trace(1_\tau)$. Prove that} \cite{etingof-tensor}
\[
d_\tau^2=1+d_\tau
\]
\textit{and, using positivity, that}
\[
d_\tau=\frac{1+\sqrt5}{2}.
\]
\textit{Let}
\[
V_n=\operatorname{Hom}_{\mathcal C}(1,\tau^{\otimes n}).
\]
\textit{Show that the dimensions of these fusion spaces satisfy the Fibonacci recurrence and determine their asymptotic growth rate.}
\end{exercise}

Let
\[
V=\operatorname{Hom}_{\mathcal C}(\tau,\tau^{\otimes3}).
\]
Set $\varphi=(1+\sqrt5)/2$. In the basis indexed by the intermediate labels $1,\tau$, define \cite{bonderson}
\[
F=
\begin{pmatrix}
\varphi^{-1}&\varphi^{-1/2}\\
\varphi^{-1/2}&-\varphi^{-1}
\end{pmatrix},
\qquad
R=
\begin{pmatrix}
e^{-4\pi i/5}&0\\
0&e^{3\pi i/5}
\end{pmatrix}.
\]
Define $\sigma_1=R$ and $\sigma_2=F^{-1}RF$. Let
\[
B_3=\langle b_1,b_2\mid b_1b_2b_1=b_2b_1b_2\rangle.
\]

\begin{exercise}[Non-Abelian braid representation]\leavevmode\par
\textit{Use the fusion rule to prove that $V$ is two-dimensional with basis indexed by the intermediate objects $1,\tau$ in $(\tau\otimes\tau)\otimes\tau$. Working exactly, verify that $F$ and $R$ are unitary and prove that}
\[
\sigma_1\sigma_2\sigma_1=\sigma_2\sigma_1\sigma_2.
\]
\textit{Prove also that $\sigma_1\sigma_2\neq\sigma_2\sigma_1$. Deduce that $b_i\mapsto\sigma_i$ defines a non-Abelian representation}
\[
B_3\longrightarrow U(V).
\]
\end{exercise}

Such Fibonacci fusion and braiding data have been implemented on superconducting quantum processors \cite{xu-fibonacci}.\footnote{This is an engineered quantum simulation of Fibonacci topological order, not evidence that naturally occurring Fibonacci quasiparticles have been conclusively observed. A related trapped-ion realization of non-Abelian topological order is reported in \cite{iqbal-anyons}.}

\section{Analysis}

\subsection{Norms, operator exponentials, and linear evolution}

A \textbf{norm} on a complex vector space $H$ is a function $\|\mathord{\cdot}\|:H\to[0,\infty)$ that is definite, homogeneous, and satisfies the triangle inequality. A normed space is a \textbf{Banach space} if every Cauchy sequence converges. A \textbf{Hilbert space} is a complex vector space with a Hermitian inner product $\langle\mathord{\cdot},\mathord{\cdot}\rangle$ whose norm $\|x\|=\sqrt{\langle x,x\rangle}$ is complete. A linear map $A:H\to K$ is \textbf{bounded} if
\[
\|A\|=\sup_{\|x\|=1}\|Ax\|<\infty.
\]
Write $\mathcal B(H,K)$ for the normed space of bounded operators and $\mathcal B(H)=\mathcal B(H,H)$.

For $A\in\mathcal B(H)$, define the operator exponential by the formal series
\[
e^A=\sum_{n=0}^{\infty}\frac{A^n}{n!}
\]
whenever the series converges in operator norm.

\begin{exercise}[Cauchy--Schwarz inequality]\leavevmode\par
\textit{Prove that every inner-product space satisfies}
\[
|\langle x,y\rangle|\leq\|x\|\|y\|.
\]
\textit{Deduce the triangle inequality for the induced norm. Prove that $\mathcal B(H,K)$ is complete when $K$ is complete and that the exponential series converges in $\mathcal B(H)$ for every bounded $A$.}
\end{exercise}

\begin{exercise}[Operator exponentials and linear evolution]\leavevmode\par
\textit{Prove that $e^{tA}$ is differentiable in operator norm and that}
\[
\frac{d}{dt}e^{tA}=Ae^{tA}=e^{tA}A.
\]
\textit{Deduce that the unique solution of}
\[
\dot x(t)=Ax(t),\qquad x(0)=x_0,
\]
\textit{is $x(t)=e^{tA}x_0$. In finite dimensions use the Jordan decomposition}
\[
A=S+N,\qquad [S,N]=0,
\]
\textit{with $S$ semisimple and $N$ nilpotent, to prove}
\[
e^{tA}=e^{tS}e^{tN}.
\]
\textit{Show that on the generalized $\lambda$-eigenspace every component of a solution is a linear combination of terms $t^je^{\lambda t}$.}
\end{exercise}

\subsection{Hilbert-space operator theory}

An \textbf{adjoint} of $A\in\mathcal B(H,K)$ is an operator $A^*\in\mathcal B(K,H)$ satisfying
\[
\langle Ax,y\rangle=\langle x,A^*y\rangle.
\]
An operator $A\in\mathcal B(H)$ is \textbf{self-adjoint} if $A=A^*$, \textbf{normal} if $A^*A=AA^*$, \textbf{positive} if $\langle x,Ax\rangle\geq0$ for every $x$, and \textbf{unitary} if $A^*A=AA^*=1$.

\begin{exercise}[Spectral theorem and singular-value decomposition]\leavevmode\par
\textit{Prove that every bounded operator between Hilbert spaces has a unique adjoint and establish the identities $(AB)^*=B^*A^*$ and $\|A^*A\|=\|A\|^2$. Let $H$ be finite-dimensional. Prove that $A\in\End(H)$ is positive if and only if $A=B^*B$ for some $B$, and prove that every normal $A$ has an orthogonal spectral decomposition}
\[
A=\sum_\lambda\lambda p_\lambda,
\qquad
p_\lambda p_\mu=\delta_{\lambda\mu}p_\lambda,
\qquad
\sum_\lambda p_\lambda=1.
\]
\textit{Apply the theorem to $T^*T$ to prove that every map $T:H\to K$ has a singular-value decomposition}
\[
T=U\Sigma V^*.
\]
\textit{Prove that the diagonal entries of $\Sigma$ are the square roots of the nonzero eigenvalues of $T^*T$, counted with multiplicity.}
\end{exercise}

\subsection{Quantum states and uncertainty}

A finite-dimensional \textbf{quantum state} is a positive operator $\rho$ with $\trace(\rho)=1$. A unit vector $\psi$ defines the pure state $\rho_\psi=|\psi\rangle\langle\psi|$. For a self-adjoint observable $A$, define
\[
\langle A\rangle_\rho=\trace(\rho A),
\qquad
\operatorname{Var}_\rho(A)
=\trace\!\left(\rho(A-\langle A\rangle_\rho1)^2\right).
\]
For $\rho=\rho_\psi$, use the subscripts $\psi$ instead of $\rho$.

\begin{exercise}[Robertson uncertainty inequality]\leavevmode\par
\textit{For self-adjoint $A,B\in\mathcal B(H)$ and a unit vector $\psi$, prove} \cite{robertson}
\[
\operatorname{Var}_\psi(A)\operatorname{Var}_\psi(B)
\geq\frac14\left|\langle\psi,[A,B]\psi\rangle\right|^2,
\qquad [A,B]=AB-BA.
\]
\textit{Determine the equality condition.}
\end{exercise}

For a self-adjoint $H$, write $U_t=e^{-itH}$.

\begin{exercise}[Conservation from commutators]\leavevmode\par
\textit{Let $A,H$ be self-adjoint. Prove that $U_t$ is unitary and that} \cite{nielsen-chuang}
\[
\frac{d}{dt}\langle U_t\psi,A U_t\psi\rangle
=-i\langle U_t\psi,[H,A]U_t\psi\rangle.
\]
\textit{Deduce that $[H,A]=0$ if and only if the expectation of $A$ is constant under this evolution for every initial vector.}
\end{exercise}

\begin{exercise}[No-cloning theorem]\leavevmode\par
\textit{Let $b\in H$ be a unit vector and suppose that a unitary $C:H\otimes H\to H\otimes H$ satisfies $C(v\otimes b)=v\otimes v$ and $C(w\otimes b)=w\otimes w$ for unit vectors $v,w$. Prove that} \cite{wootters-zurek}
\[
\langle v,w\rangle=\langle v,w\rangle^2.
\]
\textit{Deduce that no unitary map clones every unit vector.}
\end{exercise}

\subsection{Schmidt decomposition and entanglement}

For finite-dimensional Hilbert spaces $H_A,H_B$ and $\psi\in H_A\otimes H_B$, define
\[
T_\psi:H_A^*\to H_B,
\qquad
T_\psi(\phi)=(\phi\otimes1)(\psi).
\]
A \textbf{Schmidt decomposition} of $\psi$ is an expression
\[
\psi=\sum_{i=1}^r s_i a_i\otimes b_i.
\]
where $(a_i)$ and $(b_i)$ are orthonormal families and $s_i>0$; $r$ is the \textbf{Schmidt rank}. For a unit vector $\psi$, define the reduced density operators by
\[
\rho_A=\operatorname{Tr}_{H_B}(|\psi\rangle\langle\psi|),
\qquad
\rho_B=\operatorname{Tr}_{H_A}(|\psi\rangle\langle\psi|).
\]

\begin{exercise}[Schmidt decomposition and entanglement]\leavevmode\par
\textit{Use the singular-value decomposition of $T_\psi$ to prove that every $\psi$ has a Schmidt decomposition. Prove that the nonzero eigenvalues of $\rho_A$ and $\rho_B$ are the numbers $s_i^2$, with the same multiplicities. Prove that $\psi$ is separable, meaning $\psi=a\otimes b$, exactly when its Schmidt rank is one. Deduce that the entanglement entropy is} \cite{nielsen-chuang}
\[
S(\psi)=-\operatorname{Tr}(\rho_A\log\rho_A)
=-\sum_i s_i^2\log s_i^2,
\]
\textit{with the convention $0\log0=0$.}
\end{exercise}

\subsection{CHSH, Gisin, and Tsirelson}

Let $A_0,A_1$ and $B_0,B_1$ be self-adjoint operators with eigenvalues in $\{-1,1\}$. Their \textbf{CHSH operator} is
\[
\mathcal C=A_0\otimes(B_0+B_1)+A_1\otimes(B_0-B_1).
\]
\begin{exercise}[Gisin's theorem for two qubits]\leavevmode\par
\textit{Prove that every classical local-hidden-variable assignment satisfies $|\langle\mathcal C\rangle|\leq2$. For the two-qubit state in Schmidt form}
\[
\psi_\theta=\cos\theta\,|00\rangle+\sin\theta\,|11\rangle,
\qquad 0\leq\theta\leq\frac\pi4,
\]
\textit{optimize over projective binary observables and prove that the maximal CHSH value is} \cite{gisin}
\[
2\sqrt{1+\sin^2(2\theta)}.
\]
\textit{Conclude that every entangled pure two-qubit state violates a CHSH inequality. For $\theta=\pi/4$, recover the value $2\sqrt2$.}
\end{exercise}

A \textbf{bipartite binary quantum correlation matrix} has entries
\[
c_{ij}=\langle\psi,(A_i\otimes B_j)\psi\rangle,
\]
where $\psi$ is a unit vector. The operators $A_i$ and $B_j$ are self-adjoint unitaries on finite-dimensional Hilbert spaces.

For a real matrix $M=(M_{ij})$, define
\[
\beta_{\mathrm C}(M)
=\max_{\varepsilon_i,\delta_j\in\{-1,1\}}
\left|\sum_{i,j}M_{ij}\varepsilon_i\delta_j\right|
\]
and
\[
\beta_{\mathrm Q}(M)
=\sup_c\left|\sum_{i,j}M_{ij}c_{ij}\right|,
\]
where the supremum is over bipartite binary quantum correlation matrices.

\begin{exercise}[Tsirelson's representation theorem and bound]\leavevmode\par
\textit{Prove that a finite real matrix $c=(c_{ij})$ is a bipartite binary quantum correlation matrix if and only if there are unit vectors $x_i,y_j$ in a real Hilbert space such that} \cite{tsirelson}
\[
c_{ij}=\langle x_i,y_j\rangle.
\]
\textit{Deduce that}
\[
\beta_{\mathrm Q}(M)
=\sup_{\|x_i\|=\|y_j\|=1}
\left|\sum_{i,j}M_{ij}\langle x_i,y_j\rangle\right|.
\]
\textit{For the CHSH operator, prove the Tsirelson bound $\|\mathcal C\|\leq2\sqrt2$ and exhibit observables and a state attaining equality.}
\end{exercise}

\subsection{Grothendieck inequality}

The \textbf{real Grothendieck constant} $K_G^{\mathbb R}$ is the least constant for which
\[
\beta_{\mathrm Q}(M)\leq K_G^{\mathbb R}\beta_{\mathrm C}(M)
\]
holds for every real matrix $M$.

\begin{exercise}[Grothendieck's inequality]\leavevmode\par
\textit{Prove that $K_G^{\mathbb R}<\infty$} \cite{grothendieck,pisier-grothendieck}. \textit{For}
\[
M_{\mathrm{CHSH}}=
\begin{pmatrix}1&1\\1&-1\end{pmatrix},
\]
\textit{compute $\beta_{\mathrm C}(M_{\mathrm{CHSH}})$ directly. Choose four unit vectors in $\mathbb R^2$ that attain}
\[
\beta_{\mathrm Q}(M_{\mathrm{CHSH}})=2\sqrt2
\]
\textit{and verify the ratio $\sqrt2$. Deduce that $\sqrt2\leq K_G^{\mathbb R}$ and interpret the inequality as a dimension-independent bound on quantum advantage for bipartite binary correlation Bell inequalities.}
\end{exercise}

\subsection{Von Neumann algebras and subfactors}

For $\mathcal S\subseteq\mathcal B(H)$, its \textbf{commutant} is
\[
\mathcal S'=\{T\in\mathcal B(H):Tx=xT\text{ for every }x\in\mathcal S\}.
\]
A \textbf{von Neumann algebra} is a unital $*$-subalgebra $M\subseteq\mathcal B(H)$ satisfying $M=M''$ \cite{takesaki}. A net $(T_\lambda)$ converges to $T$ in the \textbf{strong operator topology} if $T_\lambda\xi\to T\xi$ for every $\xi\in H$, and in the \textbf{weak operator topology} if
\[
\langle T_\lambda\xi,\eta\rangle\longrightarrow\langle T\xi,\eta\rangle
\]
for every $\xi,\eta\in H$.

\begin{exercise}[Bicommutant theorem]\leavevmode\par
\textit{Let $A\subseteq\mathcal B(H)$ be a unital $*$-subalgebra. Prove that}
\[
A''
=\overline A^{\,\mathrm{SOT}}
=\overline A^{\,\mathrm{WOT}}.
\]
\end{exercise}

The \textbf{center} of a von Neumann algebra is $Z(M)=M\cap M'$, and $M$ is a \textbf{factor} if $Z(M)=\mathbb C1$. A positive functional or positive map is \textbf{normal} if it preserves suprema of bounded increasing nets of positive elements, and a positive functional $\psi$ is \textbf{faithful} if $\psi(a^*a)=0$ implies $a=0$. A \textbf{type $\mathrm{II}_1$ factor} is an infinite-dimensional factor with a faithful normal tracial state
\[
\tau:M\longrightarrow\mathbb C,
\qquad
\tau(ab)=\tau(ba),
\qquad
\tau(1)=1.
\]

For projections $p,q\in M$, write $p\sim q$ if there is $v\in M$ such that
\[
v^*v=p,
\qquad
vv^*=q.
\]
Write $p\precsim q$ if $p$ is equivalent to a projection $r$ satisfying $rq=r$.

\begin{exercise}[Projection dimension]\leavevmode\par
\textit{Let $M$ be a type $\mathrm{II}_1$ factor with normalized trace $\tau$. Prove that}
\[
p\sim q\quad\Longrightarrow\quad\tau(p)=\tau(q),
\]
\textit{and prove the comparison theorem}
\[
p\precsim q
\quad\Longleftrightarrow\quad
\tau(p)\leq\tau(q).
\]
\textit{Deduce that $\tau(p)=\tau(q)$ if and only if $p\sim q$.}
\end{exercise}

A von Neumann algebra is \textbf{finite} if $v^*v=1$ implies $vv^*=1$. For a linear map $\Phi:M\to N$, define its matrix amplification by $\Phi_n([x_{ij}])=[\Phi(x_{ij})]$; the map is \textbf{completely positive} if every $\Phi_n$ is positive. Let $N\subseteq M$ be finite von Neumann algebras with a common normalized trace $\tau$. A \textbf{trace-preserving conditional expectation} is a normal unital completely positive map $E_N:M\to N$ such that
\[
\tau\circ E_N=\tau,
\qquad
E_N(n_1xn_2)=n_1E_N(x)n_2
\]
for $x\in M$ and $n_1,n_2\in N$. Let $L^2(M,\tau)$ be the completion of $M$ for
\[
\langle x,y\rangle_\tau=\tau(y^*x).
\]
Let $M$ act on $L^2(M,\tau)$ by left multiplication.

\begin{exercise}[Conditional expectation and Jones projection]\leavevmode\par
\textit{Prove that there is a unique trace-preserving conditional expectation $E_N:M\to N$. Prove that $E_N$ extends to the orthogonal projection}
\[
e_N:L^2(M,\tau)\longrightarrow L^2(N,\tau).
\]
\textit{Prove that}
\[
e_N=e_N^*=e_N^2,
\qquad
e_Nxe_N=E_N(x)e_N
\]
\textit{for every $x\in M$.}
\end{exercise}

The \textbf{basic construction} is
\[
M_1=\langle M,e_N\rangle''
\subseteq\mathcal B(L^2(M,\tau)),
\]
the double commutant of the algebra generated by $M$ and $e_N$. For a projection $p=[p_{ij}]\in M_k(N)$ acting by left multiplication, define
\[
\dim_N\!\left(pL^2(N,\tau)^k\right)=\sum_{i=1}^k\tau(p_{ii}).
\]
An inclusion $N\subseteq M$ has \textbf{finite Jones index} if $L^2(M,\tau)$ is isomorphic as a right $N$-module to such a module; then
\[
[M:N]=\dim_NL^2(M,\tau).
\]

\begin{exercise}[Jones basic construction]\leavevmode\par
\textit{Let $N\subseteq M$ be an inclusion of type $\mathrm{II}_1$ factors of finite Jones index. Prove that $M_1$ is a finite factor and that its normalized trace satisfies} \cite{jones-sunder}
\[
\tau_1(xe_Ny)
=\frac{1}{[M:N]}\tau(xy)
\]
\textit{for $x,y\in M$.}
\end{exercise}

The \textbf{Jones tower} is the sequence obtained by iterating the basic construction:
\[
N=M_{-1}\subset M=M_0\subset M_1\subset M_2\subset\cdots.
\]
Let $e_i$ be the Jones projection used to construct $M_i$ from $M_{i-1}$, and set
\[
\delta=\sqrt{[M:N]}.
\]
The \textbf{Temperley--Lieb algebra} with loop parameter $\delta$ is generated by $U_i$ subject to
\[
U_i^2=\delta U_i,
\qquad
U_iU_j=U_jU_i\quad(|i-j|\geq2),
\qquad
U_iU_{i\pm1}U_i=U_i.
\]

\begin{exercise}[Temperley--Lieb relations]\leavevmode\par
\textit{Prove that the Jones projections satisfy} \cite{temperley-lieb,jones-index}
\[
e_i^2=e_i=e_i^*,
\qquad
e_ie_j=e_je_i\quad(|i-j|\geq2),
\]
\[
e_ie_{i\pm1}e_i=\delta^{-2}e_i.
\]
\textit{Deduce that $U_i\mapsto\delta e_i$ defines a representation of the Temperley--Lieb algebra.}
\end{exercise}

Let $q\in\mathbb C^\times$ satisfy $\delta=q+q^{-1}$ and define
\[
\sigma_i=q\,1-U_i.
\]
These operators reconnect the Temperley--Lieb construction with the braid groups introduced in Algebra.

\begin{exercise}[Braid representation]\leavevmode\par
\textit{Prove that $\sigma_i$ is invertible with inverse $q^{-1}1-U_i$, and prove that}
\[
\sigma_i\sigma_{i+1}\sigma_i
=\sigma_{i+1}\sigma_i\sigma_{i+1},
\]
\[
\sigma_i\sigma_j=\sigma_j\sigma_i
\qquad
(|i-j|\geq2).
\]
\textit{Deduce that the $\sigma_i$ define a representation of the braid group} \cite{jones-polynomial}.
\end{exercise}

A \textbf{Markov trace} on the Temperley--Lieb tower is a compatible family of positive normalized traces satisfying
\[
\operatorname{tr}(xU_i)=\delta^{-1}\operatorname{tr}(x)
\]
whenever $x$ belongs to the algebra generated by the preceding generators. Define
\[
P_0(\delta)=1,
\qquad
P_1(\delta)=\delta,
\qquad
P_{n+1}(\delta)=\delta P_n(\delta)-P_{n-1}(\delta).
\]

\begin{exercise}[Markov trace and discrete index values]\leavevmode\par
\textit{Prove that the normalized traces on the Jones tower restrict to a Markov trace. Prove that, when $\sin\theta\ne0$,}
\[
P_n(2\cos\theta)
=\frac{\sin((n+1)\theta)}{\sin\theta}.
\]
\textit{Use positivity of the Markov trace to prove that if $0<\delta<2$, then}
\[
\delta=2\cos\left(\frac{\pi}{m}\right)
\]
\textit{for some integer $m\geq3$.}
\end{exercise}

\begin{exercise}[Jones index theorem]\leavevmode\par
\textit{Let $N\subseteq M$ be an inclusion of type $\mathrm{II}_1$ factors of finite Jones index. Prove that} \cite{jones-index,jones-sunder}
\[
[M:N]
\in
\left\{
4\cos^2\left(\frac{\pi}{n}\right):n\geq3
\right\}
\cup
[4,\infty).
\]
\textit{For}
\[
[M:N]=4\cos^2\left(\frac{\pi}{k+2}\right),
\qquad
\delta=\sqrt{[M:N]},
\]
\textit{prove that the Jones projections determine a representation of the Temperley--Lieb algebra $\operatorname{TL}_n(\delta)$, and hence a braid-group representation. Show that the Markov trace of a braid closure satisfies the Jones skein relation. Relate the parameter}
\[
\delta=2\cos\left(\frac{\pi}{k+2}\right)
\]
\textit{to the $SU(2)_k$ fusion and braiding data from Algebra and to the Wilson-loop invariant of Chern--Simons theory from Geometry.}
\end{exercise}

\section{Geometry}

\subsection{Differential forms and cohomology}

A \textbf{smooth manifold} is a topological space equipped with an atlas whose transition maps are smooth. A smooth map $f:M\to N$ has a tangent map $df:TM\to TN$. The \textbf{tangent bundle} $TM$ is the union of the tangent spaces, the \textbf{cotangent bundle} is $T^*M$, and a \textbf{vector field} is a smooth section of $TM$ \cite{spivak}. An orientation of $M$ is a smoothly varying choice of orientation of its tangent spaces, using the determinant lines defined in Algebra.

The exterior powers $\Lambda^kT_p^*M$ are those defined in the Algebra section. To define integration in a coordinate-free way, we will integrate differential forms over chains. The space of \textbf{differential $n$-forms} is $\Omega^n(M)=\Gamma(\Lambda^nT^*M)$. On an oriented $n$-manifold, a \textbf{volume form} is a nowhere-vanishing element of $\Omega^n(M)$ that is positive with respect to the orientation. The exterior derivative $d:\Omega^n(M)\to\Omega^{n+1}(M)$ extends the differential of smooth functions and satisfies \cite{bott-tu}
\[
d(\alpha\wedge\beta)=d\alpha\wedge\beta+(-1)^p\alpha\wedge d\beta
\]
for $\alpha\in\Omega^p(M)$. A \textbf{singular $n$-simplex} is a smooth map $\sigma:\Delta^n \to M$ where $\Delta^n$ is the standard $n$-simplex. The collection of singular $n$-simplices in $M$ freely generates the abelian group $C_n(M)$ of \textbf{singular $n$-chains} in $M$. The \textbf{boundary map} $\partial : C_n(M) \to C_{n-1}(M)$ is defined on each simplex $\sigma:\Delta^n \to M$ by
$$\partial\sigma=\sum_{j=0}^{n}(-1)^j\sigma\circ f_j,$$ where $f_j : \Delta^{n-1} \to \Delta^n$ is the inclusion of the face opposite the $j$-th vertex of $\Delta^n$.

A \textbf{chain complex} $(A_n, d_n)_n$ is a sequence of abelian groups $A_n$ and homomorphisms $d_n : A_n \to A_{n-1}$ such that $d_{n-1} \circ d_n = 0$ for all $n$. The $n$-th \textbf{homology group} $H_n$ of the chain complex is
$$H_n = \ker(d_{n}) / \im(d_{n+1}).$$
The prototypical example is singular homology which corresponds to the chain complex of singular chains with the boundary map defined above.

Cohomology and cochains are defined similarly, using superscripts instead of subscripts and boundary maps $d^n : A^n \to A^{n+1}$ increase degree instead of decrease it. Singular cohomology corresponds to the cochain complex given by dualizing the singular chain complex above.

The \textbf{de Rham cohomology} of $M$ is
\[
H_{\mathrm{dR}}^n(M)
=
\ker(d\colon\Omega^n(M)\to\Omega^{n+1}(M))/
\operatorname{im}(d\colon\Omega^{n-1}(M)\to\Omega^n(M)).
\]

For a smooth map $f:N\to M$, the \textbf{pullback} of $\alpha\in\Omega^p(M)$ is
\[
(f^*\alpha)_x(v_1,\ldots,v_p)
=\alpha_{f(x)}(df_xv_1,\ldots,df_xv_p).
\]

\begin{exercise}[Poincar\'e lemma and Stokes' theorem]\leavevmode\par
\textit{Prove that $(f\circ g)^*=g^*f^*$, that pullback preserves wedge products, and that $d(f^*\alpha)=f^*(d\alpha)$. Prove that $d^2=0$. Prove the Poincar\'e lemma: every closed form is locally exact in positive degree. Define the integral of an $n$-form on an $n$-chain and prove}
\[
\int_c d\omega=\int_{\partial c}\omega.
\]
\end{exercise}

\begin{exercise}[de Rham theorem]\leavevmode\par
\textit{Prove that integration over smooth singular cycles induces a natural isomorphism between de Rham cohomology and singular cohomology with real coefficients.}
\end{exercise}

\begin{exercise}[Categorical trace on chain complexes]\leavevmode\par
\textit{Prove that bounded chain complexes of finite-dimensional vector spaces form a symmetric monoidal abelian category with duals, that the categorical trace of an endomorphism is its Lefschetz number, and that the categorical dimension of a chain complex is its Euler characteristic.}
\end{exercise}

\subsection{Bundles and K-theory}

A \textbf{smooth vector bundle} $p:E\to M$ is a smooth family of vector spaces locally isomorphic to a product $U\times V\to U$. For a smooth map $f:N\to M$, its \textbf{pullback bundle} is
\[
f^*E=\{(x,e)\in N\times E:f(x)=p(e)\}\longrightarrow N.
\]

For a compact Hausdorff space $X$, let $\operatorname{Vect}(X)$ be the category of finite-rank complex vector bundles with continuous local trivializations over $X$, with the usual direct sum, tensor product, dual, and pullback constructions \cite{hatcher-k}. The \textbf{Grothendieck group} $K^0(X)$ is generated by isomorphism classes $[E]$ subject to
\[
[E\oplus F]=[E]+[F].
\]
Define multiplication by $[E][F]=[E\otimes F]$. For a continuous map $f:X\to Y$ between compact Hausdorff spaces, define pullback by
\[
f^*:K^0(Y)\longrightarrow K^0(X),
\qquad
[E]\longmapsto[f^*E].
\]

\begin{exercise}[Grothendieck group of vector bundles]\leavevmode\par
\textit{Prove that $(f\circ g)^*E\cong g^*f^*E$ and that pullback preserves direct sums, tensor products, and duals. Let $X$ and $Y$ be compact Hausdorff spaces and let $f:X\to Y$ be continuous. Prove that direct sum and tensor product induce a commutative ring structure on $K^0(X)$ and that $f^*$ is a ring homomorphism. Prove that}
\[
K^0(\mathrm{pt})\cong\mathbb Z
\]
\textit{via the rank map.}
\end{exercise}

Fix a basepoint $x_0\in X$, regarded as a map $x_0:\mathrm{pt}\to X$. The \textbf{reduced K-theory} of the pointed space $(X,x_0)$ is
\[
\widetilde K^0(X)
=\ker\!\left(K^0(X)\xrightarrow{x_0^*}K^0(\mathrm{pt})\right).
\]
For a bundle $E$ of rank $r$, its \textbf{reduced class} is
\[
[E]-[\underline{\mathbb C}^{\,r}],
\]
where $\underline{\mathbb C}^{\,r}=X\times\mathbb C^r$ is the trivial rank-$r$ bundle. Write $K^n(X)$ for the generalized cohomology groups extending $K^0(X)$.

\begin{exercise}[Stable equivalence and Bott periodicity]\leavevmode\par
\textit{Let $E$ and $F$ be complex vector bundles over a compact Hausdorff space $X$. Prove that $[E]=[F]$ in $K^0(X)$ if and only if there is a vector bundle $G$ such that}
\[
E\oplus G\cong F\oplus G.
\]
\textit{Deduce that the reduced class of $E$ is unchanged after replacing $E$ by $E\oplus\underline{\mathbb C}^{\,n}$. Prove Bott periodicity}
\[
K^{n+2}(X)\cong K^n(X)
\]
\textit{and deduce that complex K-theory is naturally $\mathbb Z/2$-graded.}
\end{exercise}

For the gapped families below, assume that $X$ is connected, so vector-bundle ranks are constant. Let $V$ be a finite-dimensional Hermitian vector space. A continuous family is a map
\[
H:X\longrightarrow\End(V)
\]
and write $H_x=H(x)$. It is \textbf{gapped} if every $H_x$ is self-adjoint and invertible. Given a positively oriented contour $\Gamma$ enclosing the negative spectra and excluding the positive spectra, define
\[
P_x^-=\frac{1}{2\pi i}\int_\Gamma(z-H_x)^{-1}\,dz,
\qquad
E_x^-=\operatorname{im}(P_x^-).
\]

\begin{exercise}[Negative spectral bundle]\leavevmode\par
\textit{Let $H$ be a gapped family over compact $X$. Prove that there are $\delta,M>0$ such that every eigenvalue of every $H_x$ lies in $[-M,-\delta]\cup[\delta,M]$, and hence that a contour $\Gamma$ as above exists. Prove that}
\[
(P_x^-)^2=P_x^-,
\qquad
(P_x^-)^*=P_x^-,
\]
\textit{that $x\mapsto P_x^-$ is continuous, and that}
\[
E_H^-=\coprod_{x\in X}E_x^-
\]
\textit{is a complex vector bundle over $X$. Prove that its rank is locally constant.}
\end{exercise}

For a gapped family $H:X\times[0,1]\to\End(V)$, let $E_j^-$ be the negative spectral bundle of $x\mapsto H(x,j)$, and let $i_j:X\to X\times[0,1]$ be given by $i_j(x)=(x,j)$.

\begin{exercise}[Gap-preserving homotopy]\leavevmode\par
\textit{Prove that the negative spectral subspaces of $H$ form a vector bundle $E^-\to X\times[0,1]$. Prove that the endpoint pullbacks of any vector bundle over $X\times[0,1]$ are isomorphic, and apply this to $i_0^*E^-$ and $i_1^*E^-$. Deduce that}
\[
[E_0^-]=[E_1^-]\in K^0(X).
\]
\end{exercise}

Let $Q\in\End(W)$ be a fixed invertible self-adjoint endomorphism of a finite-dimensional Hermitian space $W$, let $W^-$ be its negative eigenspace, and write $\underline{W^-}=X\times W^-$.

\begin{exercise}[Stabilization]\leavevmode\par
\textit{For a gapped family $H$, prove that}
\[
E_{H\oplus Q}^-\cong E_H^-\oplus\underline{W^-}.
\]
\textit{Deduce that}
\[
[E_H^-]-[\underline{\mathbb C}^{\,\operatorname{rank}E_H^-}]
\in\widetilde K^0(X)
\]
\textit{is unchanged by adjoining a constant gapped summand.}
\end{exercise}

Let $E\subseteq X\times V$ be a subbundle and let $P_x$ be the orthogonal projection onto $E_x$.

\begin{exercise}[Flattened family]\leavevmode\par
\textit{Define $H_x=1_V-2P_x$. Prove that $x\mapsto H_x$ is a gapped family, that}
\[
H_x^2=1_V,
\]
\textit{and that its negative spectral bundle is $E$. Deduce that every subbundle of a trivial bundle arises as the negative spectral bundle of a gapped family. Combine the preceding exercises to prove that a finite-dimensional gapped family determines a class in $\widetilde K^0(X)$ invariant under gap-preserving homotopy and stabilization.}
\end{exercise}

This construction is used in the classification of noninteracting topological insulators and superconductors under specified symmetry assumptions \cite{kitaev-periodic}.

\subsection{Symplectic and metric geometry}

For a vector field $X$, contraction is the map $\iota_X:\Omega^p(M)\to\Omega^{p-1}(M)$ defined by
\[
(\iota_X\alpha)(X_2,\ldots,X_p)=\alpha(X,X_2,\ldots,X_p).
\]
The Lie derivative of forms is determined by Cartan's formula $\mathcal{L}_X=d\iota_X+\iota_Xd$.

A \textbf{symplectic form} is a two-form $\omega\in\Omega^2(M)$ such that $d\omega=0$ and $X\mapsto\iota_X\omega$ is a bundle isomorphism $TM\to T^*M$. Write $C^\infty(M)$ for the smooth real-valued functions on $M$. For $H\in C^\infty(M)$, define $X_H$ by
\[
\iota_{X_H}\omega=dH.
\]

\begin{exercise}[Liouville's theorem]\leavevmode\par
\textit{Let $(M,\omega)$ be $2n$-dimensional with $\omega$ symplectic. Prove that $\omega^n/n!$ is a volume form. For $H\in C^\infty(M)$, prove using Cartan's formula that} \cite{arnold}
\[
\mathcal{L}_{X_H}\omega=0.
\]
\textit{Deduce that $\mathcal{L}_{X_H}\omega^n=0$ and hence that the local flow of $X_H$ preserves the volume form}
\[
\frac{\omega^n}{n!}.
\]
\end{exercise}

For $f,g\in C^\infty(M)$, define
\[
\{f,g\}=\omega(X_f,X_g).
\]

The following is a Hamiltonian symmetry--conservation statement, not the full variational theorem of \cite{noether}.

\begin{exercise}[Hamiltonian Noether theorem]\leavevmode\par
\textit{Let $(M,\omega)$ be symplectic. Prove that} \cite{arnold}
\[
X_g(f)=\{f,g\}.
\]
\textit{Let $\Phi_t$ be the local flow of $X_f$. Prove that the following are equivalent:}
\begin{itemize}
\item $H\circ\Phi_t=H$ whenever both sides are defined.
\item $X_f(H)=0$.
\item $\{H,f\}=0$.
\item $f$ is constant along the integral curves of $X_H$.
\end{itemize}
\end{exercise}

A \textbf{pseudo-Riemannian metric} is a smooth nondegenerate symmetric section $g\in\Gamma(T^*M\otimes T^*M)$. On an oriented pseudo-Riemannian $n$-manifold, the metric induces a bilinear form $\langle\cdot,\cdot\rangle_g$ on differential forms. A \textbf{metric volume form} $\operatorname{vol}_g$ is a positive volume form satisfying
\[
\left|\langle\operatorname{vol}_g,\operatorname{vol}_g\rangle_g\right|=1.
\]
The \textbf{Hodge star} is an operator $\star:\Omega^p(M)\to\Omega^{n-p}(M)$ satisfying
\[
\alpha\wedge\star\beta=\langle\alpha,\beta\rangle_g\operatorname{vol}_g.
\]

\begin{exercise}[Metric volume and Hodge star]\leavevmode\par
\textit{Prove that an oriented pseudo-Riemannian manifold has a unique metric volume form and a unique Hodge-star operator. Determine $\star^2$ in terms of the degree and signature of the metric.}
\end{exercise}

\begin{exercise}[Charge conservation from Maxwell's equations]\leavevmode\par
\textit{Let $M$ be an oriented four-dimensional pseudo-Riemannian manifold. Suppose that $F\in\Omega^2(M)$ and $J\in\Omega^3(M)$ satisfy} \cite{frankel}
\[
dF=0,
\qquad
d\star F=J.
\]
\textit{Prove that $dJ=0$. If $C$ is an oriented compact four-dimensional chain, deduce that}
\[
\int_{\partial C}J=0.
\]
\textit{If the three-dimensional pieces of its boundary satisfy}
\[
\partial C=\Sigma_1-\Sigma_0+B,
\]
\textit{prove that}
\[
\int_{\Sigma_1}J-\int_{\Sigma_0}J=-\int_BJ.
\]
\end{exercise}

For a closed form $A$, write $[A]$ for its de Rham cohomology class. The next exercise is a modern de Rham formulation; the original paper does not use this language \cite{aharonov-bohm,frankel}.

\begin{exercise}[Aharonov--Bohm cohomological obstruction]
\textit{Let $M$ be a compact smooth manifold and let $A\in\Omega^1(M)$ satisfy $dA=0$. Prove the following:}
\begin{itemize}
\item For every contractible open set $U\subseteq M$, there exists $\lambda\in C^\infty(U)$ such that $A|_U=d\lambda$.
\item For every smooth closed curve $\gamma$, the number $\int_\gamma A$ depends only on the homology class of $\gamma$.
\item There exists a global $\lambda\in C^\infty(M)$ with $A=d\lambda$ iff $[A]=0\in H_{\mathrm{dR}}^1(M)$.
\item If $[A]\neq0$, then there exists a smooth closed curve $\gamma$ such that $\int_\gamma A\neq0$.
\item For a particle of charge $q$, the phase $\exp(iq\int_\gamma A)$ is gauge invariant. Explain how a nontrivial phase can shift an interference pattern even though $dA=0$ on $M$.
\end{itemize}
\end{exercise}

\subsection{Connections and curvature}

An \textbf{affine connection} is a map $\nabla : \Gamma(TM) \times \Gamma(TM) \to \Gamma(TM)$ such that
\[
\nabla_{fX}Y=f\nabla_XY,
\qquad
\nabla_X(fY)=X(f)Y+f\nabla_XY.
\]
For a covector field $\alpha$, set
\[
(\nabla_X\alpha)(Y)=X(\alpha(Y))-\alpha(\nabla_XY).
\]
Define the covariant derivative of general tensor fields by the product rule and by requiring it to commute with contractions.

A connection is \textbf{torsion-free and metric-compatible} when
\[
\nabla_XY-\nabla_YX=[X,Y],
\qquad
\nabla g=0.
\]
Such a connection is called a \textbf{Levi-Civita connection}.

\begin{exercise}[Levi-Civita connection]\leavevmode\par
\textit{Prove that every pseudo-Riemannian manifold admits a unique Levi-Civita connection. Derive the coordinate-free Koszul formula.}
\end{exercise}

An affinely parametrized curve $\gamma:I\to M$ is a \textbf{geodesic} if $\nabla_{\dot\gamma}\dot\gamma=0$. The Lie derivative of the metric is
\[
(\mathcal L_Xg)(Y,Z)
=X(g(Y,Z))-g([X,Y],Z)-g(Y,[X,Z]),
\]
and $X$ is a \textbf{Killing vector field} if $\mathcal L_Xg=0$.

Let $(M,g)$ be a pseudo-Riemannian manifold with Levi-Civita connection $\nabla$ \cite{oneill,wald}.

\begin{exercise}[Killing fields and conserved quantities]\leavevmode\par
\textit{Let $X$ be a Killing vector field and let $\gamma$ be an affinely parametrized geodesic. Prove that}
\[
\frac{d}{dt}g(\dot\gamma,\dot\gamma)=0,
\qquad
\frac{d}{dt}g(X,\dot\gamma)=0.
\]
\textit{Deduce that $g(X,\dot\gamma)$ is constant along $\gamma$.}
\end{exercise}

The \textbf{curvature} of $\nabla$ is the map
\[
R(X,Y)Z
=\nabla_X\nabla_YZ-\nabla_Y\nabla_XZ-\nabla_{[X,Y]}Z.
\]

\begin{exercise}[Curvature as a tensor]\leavevmode\par
\textit{For $f\in C^\infty(M)$ and vector fields $X,Y,Z$, prove that}
\[
R(fX,Y)Z=fR(X,Y)Z,
\qquad
R(X,fY)Z=fR(X,Y)Z,
\]
\[
R(X,Y)(fZ)=fR(X,Y)Z.
\]
\textit{Prove that $R(X,Y)=-R(Y,X)$ and deduce that}
\[
R\in\Gamma\!\left(\Lambda^2T^*M\otimes T^*M\otimes TM\right).
\]
\end{exercise}

Let $\Gamma:(-\varepsilon,\varepsilon)\times I\to M$ be a smooth variation through affinely parametrized geodesics, and define the vector fields along $\Gamma$ by
\[
T=\frac{\partial\Gamma}{\partial t},
\qquad
J=\frac{\partial\Gamma}{\partial s}.
\]

\begin{exercise}[Geodesic deviation]
\textit{For a variation $\Gamma$ as above, prove that $[T,J]=0$ and that}
\[
\nabla_T\nabla_TJ=R(T,J)T.
\]
\end{exercise}

The trace used below is the ordinary trace defined in the Algebra section. Define the \textbf{Ricci tensor}, \textbf{scalar curvature}, and \textbf{Einstein tensor} by
\[
\operatorname{Ric}(X,Y)=\operatorname{tr}\bigl(Z\mapsto R(Z,X)Y\bigr),
\qquad
S=\operatorname{tr}_g(\operatorname{Ric}),
\qquad
G=\operatorname{Ric}-\frac12Sg.
\]
For a symmetric covariant two-tensor $A$, its \textbf{divergence} is the covector field
\[
(\operatorname{div}A)(Y)
=\operatorname{tr}_g\bigl((X,Z)\mapsto(\nabla_XA)(Z,Y)\bigr).
\]

\begin{exercise}[Contracted Bianchi identity and conservation]\leavevmode\par
\textit{Assume the second Bianchi identity}
\[
(\nabla_XR)(Y,Z)+(\nabla_YR)(Z,X)+(\nabla_ZR)(X,Y)=0.
\]
\textit{Contract this identity to prove that}
\[
\operatorname{div}G=0.
\]
\textit{Deduce that if $T$ is a symmetric covariant two-tensor satisfying}
\[
G+\Lambda g=\kappa T
\]
\textit{for constants $\Lambda,\kappa$ with $\kappa\ne0$, then $\operatorname{div}T=0$.}
\end{exercise}

Connections and curvature also determine characteristic classes. In odd dimensions, their transgression forms define Chern--Simons functionals.

Let $E\to M$ be a complex vector bundle and write $\Omega^0(M,E)=\Gamma(E)$. A \textbf{connection} on $E$ is a $\mathbb C$-linear map
\[
\nabla:\Omega^0(M,E)\longrightarrow\Omega^1(M,E)
\]
satisfying $\nabla(fs)=df\otimes s+f\nabla s$. For $\alpha\in\Omega^p(M)$ and an $E$-valued form $\eta$, extend it by
\[
\nabla(\alpha\wedge\eta)
=d\alpha\wedge\eta+(-1)^p\alpha\wedge\nabla\eta.
\]
Its \textbf{curvature} is
\[
F_\nabla=\nabla^2\in\Omega^2(M,\End E).
\]
The connection induces one on $\End E$-valued forms.

\begin{exercise}[Bianchi identity]
\textit{Let $\nabla$ be a connection on $E\to M$. Prove that}
\[
\nabla F_\nabla=0.
\]
\end{exercise}

Let $H:X\to\End(V)$ be a smooth family of self-adjoint operators with a simple isolated eigenvalue $\lambda_x$, and set
\[
L=\{(x,v)\in X\times V:H_xv=\lambda_xv\}.
\]
For a local unit eigenvector $\psi$, define the \textbf{Berry connection} and \textbf{Berry curvature} by
\[
\mathcal A=i\langle\psi,d\psi\rangle,
\qquad
\mathcal F=d\mathcal A.
\]

\begin{exercise}[Berry phase as holonomy]\leavevmode\par
\textit{Prove that $L\to X$ is a complex line bundle. Under the change $\psi\mapsto e^{i\lambda}\psi$, prove that $\mathcal A\mapsto\mathcal A-d\lambda$ and that $\mathcal F$ is unchanged. For a closed curve $\gamma$ in $X$, prove that}
\[
\exp\left(i\int_\gamma\mathcal A\right)
\]
\textit{is the holonomy of the eigenline connection and is independent of the local choice of eigenvector. Interpret it as the geometric phase accumulated during adiabatic transport.}
\end{exercise}

Let $P$ be an invariant symmetric $k$-linear polynomial on the endomorphism algebra of a fiber of $E$, where invariant means invariant under simultaneous conjugation. Define \cite{chern-simons}
\[
P(F_\nabla)
=P(F_\nabla,\ldots,F_\nabla)
\in\Omega^{2k}(M).
\]

\begin{exercise}[Chern--Weil theorem]\leavevmode\par
\textit{Prove that}
\[
dP(F_\nabla)=0.
\]
\textit{For two connections $\nabla^0,\nabla^1$ on $E$, prove that}
\[
[P(F_{\nabla^0})]
=[P(F_{\nabla^1})]
\in H_{\mathrm{dR}}^{2k}(M).
\]
\end{exercise}

Define the \textbf{Chern character form} by
\[
\operatorname{ch}(E,\nabla)
=\operatorname{tr}\exp\left(\frac{iF_\nabla}{2\pi}\right).
\]
When induced connections are understood, suppress them from the notation.

\begin{exercise}[Chern character]\leavevmode\par
\textit{Prove that $d\,\operatorname{ch}(E,\nabla)=0$ and that its de Rham class is independent of $\nabla$. For a second complex vector bundle $F$ with connection, prove that}
\[
\operatorname{ch}(E\oplus F)
=\operatorname{ch}(E)+\operatorname{ch}(F),
\]
\[
\operatorname{ch}(E\otimes F)
=\operatorname{ch}(E)\wedge\operatorname{ch}(F).
\]
\textit{If $M$ is compact, deduce that the Chern character defines a ring homomorphism}
\[
\operatorname{ch}:K^0(M)\longrightarrow H_{\mathrm{dR}}^{\mathrm{even}}(M),
\qquad
H_{\mathrm{dR}}^{\mathrm{even}}(M)=\bigoplus_{j\geq0}H_{\mathrm{dR}}^{2j}(M).
\]
\end{exercise}

\subsection{Chern--Simons theory and TQFT}

For two connections on $E$, set
\[
\nabla^t=(1-t)\nabla^0+t\nabla^1,
\qquad
A=\nabla^1-\nabla^0,
\qquad
F_t=F_{\nabla^t},
\]
where $A\in\Omega^1(M,\End E)$, and define
\[
\operatorname{CS}_P(\nabla^0,\nabla^1)
=k\int_0^1P(A,F_t,\ldots,F_t)\,dt.
\]
This defines $\operatorname{CS}_P(\nabla^0,\nabla^1)\in\Omega^{2k-1}(M)$.

\begin{exercise}[Chern--Simons transgression]\leavevmode\par
\textit{Prove that}
\[
P(F_{\nabla^1})-P(F_{\nabla^0})
=d\,\operatorname{CS}_P(\nabla^0,\nabla^1).
\]
\end{exercise}

Let $\mathfrak g\subseteq M_N(\mathbb C)$ be a matrix Lie algebra whose trace pairing is nondegenerate. For $A\in\Omega^1(M,\mathfrak g)$, wedge products include matrix multiplication. Define
\[
F_A=dA+A\wedge A
\]
and
\[
\operatorname{CS}(A)
=\operatorname{tr}\left(
A\wedge dA+\frac23A\wedge A\wedge A
\right).
\]

\begin{exercise}[Chern--Simons form]\leavevmode\par
\textit{Prove that}
\[
d\,\operatorname{CS}(A)
=\operatorname{tr}(F_A\wedge F_A).
\]
\end{exercise}

For a closed oriented three-manifold $M$, define
\[
S_{\mathrm{CS}}(A)=\int_M\operatorname{CS}(A).
\]

\begin{exercise}[Chern--Simons equation]\leavevmode\par
\textit{For $\alpha\in\Omega^1(M,\mathfrak g)$, set $A_t=A+t\alpha$. Prove that}
\[
\left.\frac{d}{dt}\right|_{t=0}S_{\mathrm{CS}}(A_t)
=2\int_M\operatorname{tr}(\alpha\wedge F_A).
\]
\textit{Deduce that the critical points satisfy $F_A=0$.}
\end{exercise}

Let $G$ be a matrix Lie group with Lie algebra $\mathfrak g$. For a smooth map $g:M\to G$, define
\[
A^g=g^{-1}Ag+g^{-1}dg.
\]
Assume that $G$ is compact, connected, simple, and simply connected, and normalize the invariant trace so that
\[
\nu(g)=\frac{1}{24\pi^2}\int_M
\operatorname{tr}\bigl((g^{-1}dg)^3\bigr)\in\mathbb Z.
\]
For $k\in\mathbb R$, set $S_k=(k/4\pi)S_{\mathrm{CS}}$.

\begin{exercise}[Gauge transformation]\leavevmode\par
\textit{Prove that}
\[
\operatorname{CS}(A^g)-\operatorname{CS}(A)
=-d\,\operatorname{tr}(dg\,g^{-1}\wedge A)
-\frac13\operatorname{tr}\bigl((g^{-1}dg)^3\bigr).
\]
\textit{Deduce that $\exp(iS_k)$ is invariant under all gauge transformations when $k\in\mathbb Z$.}
\end{exercise}

Now restrict to $2+1$ spacetime dimensions. Let $M$ be a closed oriented three-manifold and let $\ell>0$, so the cosmological constant is $\Lambda=-\ell^{-2}$. After identifying the coframe representation with $\mathfrak{sl}(2,\mathbb R)$, let
\[
e,\omega\in\Omega^1(M,\mathfrak{sl}(2,\mathbb R)),
\qquad
A^\pm=\omega\pm\frac1\ell e.
\]
The pair $(A^+,A^-)$ has gauge Lie algebra $\mathfrak{sl}(2,\mathbb R)\oplus\mathfrak{sl}(2,\mathbb R)$.
Write
\[
d_\omega e=de+\omega\wedge e+e\wedge\omega,
\qquad
F_\omega=d\omega+\omega\wedge\omega.
\]

\begin{exercise}[Three-dimensional gravity]\leavevmode\par
\textit{Prove that $F_{A^+}=F_{A^-}=0$ is equivalent to}
\[
d_\omega e=0,
\qquad
F_\omega+\frac1{\ell^2}e\wedge e=0.
\]
\textit{Assuming that $e:TM\to\mathfrak{sl}(2,\mathbb R)$ is fiberwise invertible, identify these equations with the torsion-free condition and the Einstein equation with constant negative curvature.}
\end{exercise}

Define the first-order three-dimensional Einstein--Hilbert functional, up to its conventional overall coefficient, by
\[
S_{\mathrm{EH}}(e,\omega)
=\int_M\operatorname{tr}\left(
e\wedge F_\omega+\frac{1}{3\ell^2}e\wedge e\wedge e
\right).
\]

\begin{exercise}[Gravity as Chern--Simons theory]\leavevmode\par
\textit{Prove that, up to an overall normalization and an exact term,}
\[
S_{\mathrm{CS}}(A^+)-S_{\mathrm{CS}}(A^-)
\]
\textit{equals $S_{\mathrm{EH}}(e,\omega)$} \cite{achucarro-townsend,witten-gravity}.
\end{exercise}

This is a local Lie-algebra formulation of gravity in three spacetime dimensions. Changing between Lorentzian and Euclidean signatures changes the relevant real form and reality conditions. It is an analytic continuation, not a statement that four-dimensional gravity is obtained from Chern--Simons theory by Wick rotation.

For a connection $A$ and a finite-dimensional representation $V$ of its gauge group, parallel transport around an oriented closed curve $\gamma$ gives the holonomy $\operatorname{Hol}_\gamma(A)$. Define the \textbf{Wilson loop}
\[
W_V(\gamma,A)
=\operatorname{tr}_V\!\left(\operatorname{Hol}_\gamma(A)\right).
\]
For a framed oriented link $L=\gamma_1\sqcup\cdots\sqcup\gamma_r$ labeled by $V_1,\ldots,V_r$, set
\[
W_L(A)=\prod_{j=1}^rW_{V_j}(\gamma_j,A).
\]
For a framed link, define the formal Chern--Simons expectation value \cite{witten-jones}
\[
\langle W_L\rangle_k
=
\frac{\int e^{iS_k(A)}W_L(A)\,\mathcal DA}
{\int e^{iS_k(A)}\,\mathcal DA}
\]
For normalized $SU(2)$ theory at integer level $k\geq1$, set
\[
q=\exp\left(\frac{2\pi i}{k+2}\right).
\]

\begin{exercise}[Wilson loops and the Jones polynomial]\leavevmode\par
\textit{Prove that the expectation value depends on the chosen framing. Let $L_+$, $L_-$, and $L_0$ differ only by a positive crossing, a negative crossing, and the oriented smoothing. Assume that, after the standard framing normalization, the Wilson-loop expectation values satisfy}
\[
q^{-1}\langle W_{L_+}\rangle_k
-q\langle W_{L_-}\rangle_k
=\left(q^{1/2}-q^{-1/2}\right)\langle W_{L_0}\rangle_k
\]
\textit{and $\langle W_{\bigcirc}\rangle_k=1$. Prove that these relations determine the Jones polynomial of an oriented link.}
\end{exercise}

Let $\operatorname{Bord}_3^{\mathrm{or}}$ be the symmetric monoidal category whose objects are closed oriented surfaces, whose morphisms are oriented three-dimensional bordisms modulo diffeomorphism, and whose tensor product is disjoint union. A \textbf{three-dimensional oriented TQFT} is a symmetric monoidal functor
\[
Z:\operatorname{Bord}_3^{\mathrm{or}}
\longrightarrow\operatorname{Vect}_{\mathbb C}.
\]

\begin{exercise}[Reshetikhin--Turaev TQFT]\leavevmode\par
\textit{Let $\mathcal C$ be a modular braided fusion category. Prove that the Reshetikhin--Turaev construction assigns finite-dimensional state spaces to closed oriented surfaces and compatible linear maps to bordisms, defining a three-dimensional oriented TQFT} \cite{reshetikhin-turaev}. \textit{Prove that labeled Wilson lines induce the braid-group actions determined by the $F$- and $R$-symbols of $\mathcal C$. For the $SU(2)_k$ category, relate the resulting link invariant to the Temperley--Lieb and Jones constructions in Analysis and to the Chern--Simons Wilson-loop skein relation above.}
\end{exercise}

\begin{thebibliography}{99}

\bibitem{halmos}
Paul R. Halmos,
\textit{Finite-Dimensional Vector Spaces},
2nd ed., Springer, 1974.

\bibitem{hoffman-kunze}
Kenneth Hoffman and Ray Kunze,
\textit{Linear Algebra},
2nd ed., Prentice Hall, 1971.

\bibitem{spivak}
Michael Spivak,
\textit{Calculus on Manifolds: A Modern Approach to Classical Theorems of Advanced Calculus},
Addison-Wesley, 1965.

\bibitem{lang}
Serge Lang,
\textit{Algebra},
3rd ed., Springer, 2002.

\bibitem{serre-representations}
Jean-Pierre Serre,
\textit{Linear Representations of Finite Groups},
Graduate Texts in Mathematics, Vol. 42, Springer, 1977.

\bibitem{cotton-chemical}
F. Albert Cotton,
\textit{Chemical Applications of Group Theory},
3rd ed., Wiley, 1990.

\bibitem{ponto-shulman}
Kate Ponto and Michael Shulman,
\textit{Traces in symmetric monoidal categories},
Expositiones Mathematicae, 32(3):248--273, 2014.

\bibitem{bott-tu}
Raoul Bott and Loring W. Tu,
\textit{Differential Forms in Algebraic Topology},
Graduate Texts in Mathematics, Vol. 82, Springer, 1982.

\bibitem{hatcher-k}
Allen Hatcher,
\textit{Vector Bundles and K-Theory},
Version 2.2, November 2017.

\bibitem{kitaev-periodic}
Alexei Kitaev,
\textit{Periodic table for topological insulators and superconductors},
AIP Conference Proceedings, 1134:22--30, 2009.
doi:10.1063/1.3149495.

\bibitem{takesaki}
Masamichi Takesaki,
\textit{Theory of Operator Algebras I},
Encyclopaedia of Mathematical Sciences, Vol. 124,
Springer, 2002.

\bibitem{jones-sunder}
Vaughan F. R. Jones and V. S. Sunder,
\textit{Introduction to Subfactors},
London Mathematical Society Lecture Note Series, Vol. 234,
Cambridge University Press, 1997.
doi:10.1017/CBO9780511566219.

\bibitem{jones-index}
Vaughan F. R. Jones,
\textit{Index for subfactors},
Inventiones Mathematicae, 72(1):1--25, 1983.
doi:10.1007/BF01389127.

\bibitem{jones-polynomial}
Vaughan F. R. Jones,
\textit{A polynomial invariant for knots via von Neumann algebras},
Bulletin of the American Mathematical Society, 12(1):103--111, 1985.
doi:10.1090/S0273-0979-1985-15304-2.

\bibitem{temperley-lieb}
H. N. V. Temperley and E. H. Lieb,
\textit{Relations between the ``percolation'' and ``colouring'' problem and other graph-theoretical problems associated with regular planar lattices},
Proceedings of the Royal Society A, 322(1549):251--280, 1971.
doi:10.1098/rspa.1971.0067.

\bibitem{nielsen-chuang}
Michael A. Nielsen and Isaac L. Chuang,
\textit{Quantum Computation and Quantum Information},
10th Anniversary ed., Cambridge University Press, 2010.

\bibitem{gisin}
Nicolas Gisin,
\textit{Bell's inequality holds for all non-product states},
Physics Letters A, 154(5--6):201--202, 1991.
doi:10.1016/0375-9601(91)90805-I.

\bibitem{tsirelson}
B. S. Tsirelson,
\textit{Quantum generalizations of Bell's inequality},
Letters in Mathematical Physics, 4:93--100, 1980.
doi:10.1007/BF00417500.

\bibitem{grothendieck}
Alexander Grothendieck,
\textit{R\'esum\'e de la th\'eorie m\'etrique des produits tensoriels topologiques},
Boletim da Sociedade de Matem\'atica de S\~ao Paulo, 8:1--79, 1953.

\bibitem{pisier-grothendieck}
Gilles Pisier,
\textit{Grothendieck's theorem, past and present},
Bulletin of the American Mathematical Society, 49(2):237--323, 2012.
doi:10.1090/S0273-0979-2011-01348-9.

\bibitem{wootters-zurek}
W. K. Wootters and W. H. Zurek,
\textit{A Single Quantum Cannot Be Cloned},
Nature, 299:802--803, 1982.
doi:10.1038/299802a0.

\bibitem{robertson}
H. P. Robertson,
\textit{The Uncertainty Principle},
Physical Review, 34:163--164, 1929.
doi:10.1103/PhysRev.34.163.

\bibitem{arnold}
V. I. Arnold,
\textit{Mathematical Methods of Classical Mechanics},
2nd ed., Springer, 1989.

\bibitem{noether}
Emmy Noether,
\textit{Invariant Variation Problems},
translated by M. A. Tavel,
Transport Theory and Statistical Physics, 1(3):186--207, 1971;
originally published 1918.
doi:10.1080/00411457108231446.

\bibitem{frankel}
Theodore Frankel,
\textit{The Geometry of Physics: An Introduction},
3rd ed., Cambridge University Press, 2011.
doi:10.1017/CBO9781139061377.

\bibitem{oneill}
Barrett O'Neill,
\textit{Semi-Riemannian Geometry with Applications to Relativity},
Pure and Applied Mathematics, Vol. 103, Academic Press, 1983.

\bibitem{wald}
Robert M. Wald,
\textit{General Relativity},
University of Chicago Press, 1984.

\bibitem{chern-simons}
S. S. Chern and J. Simons,
\textit{Characteristic forms and geometric invariants},
Annals of Mathematics, 99(1):48--69, 1974.
doi:10.2307/1971013.

\bibitem{witten-jones}
E. Witten,
\textit{Quantum field theory and the Jones polynomial},
Commun. Math. Phys., 121(3):351--399, 1989.
doi:10.1007/BF01217730.

\bibitem{reshetikhin-turaev}
Nicolai Reshetikhin and Vladimir G. Turaev,
\textit{Ribbon graphs and their invariants derived from quantum groups},
Communications in Mathematical Physics, 127(1):1--26, 1990.
doi:10.1007/BF02096491.

\bibitem{achucarro-townsend}
A. Ach\'ucarro and P. K. Townsend,
\textit{A Chern--Simons action for three-dimensional anti-de Sitter supergravity theories},
Physics Letters B, 180(1--2):89--92, 1986.
doi:10.1016/0370-2693(86)90140-1.

\bibitem{witten-gravity}
Edward Witten,
\textit{$2+1$ dimensional gravity as an exactly soluble system},
Nuclear Physics B, 311(1):46--78, 1988.
doi:10.1016/0550-3213(88)90143-5.

\bibitem{aharonov-bohm}
Yakir Aharonov and David Bohm,
\textit{Significance of Electromagnetic Potentials in the Quantum Theory},
Physical Review, 115(3):485--491, 1959.
doi:10.1103/PhysRev.115.485.

\bibitem{etingof-tensor}
Pavel Etingof, Shlomo Gelaki, Dmitri Nikshych, and Victor Ostrik,
\textit{Tensor Categories},
Mathematical Surveys and Monographs, Vol. 205,
American Mathematical Society, 2015.
doi:10.1090/surv/205.

\bibitem{bonderson}
Parsa H. Bonderson,
\textit{Non-Abelian Anyons and Interferometry},
Ph.D. thesis, California Institute of Technology, 2007.
doi:10.7907/5NDZ-W890.

\bibitem{nakamura-anyons}
J. Nakamura, S. Liang, G. C. Gardner, and M. J. Manfra,
\textit{Direct observation of anyonic braiding statistics},
Nature Physics, 16:931--936, 2020.
doi:10.1038/s41567-020-1019-1.

\bibitem{iqbal-anyons}
M. Iqbal et al.,
\textit{Non-Abelian topological order and anyons on a trapped-ion processor},
Nature, 626:505--511, 2024.
doi:10.1038/s41586-023-06934-4.

\bibitem{xu-fibonacci}
S. Xu et al.,
\textit{Non-Abelian braiding of Fibonacci anyons with a superconducting processor},
Nature Physics, 20:1469--1475, 2024.
doi:10.1038/s41567-024-02529-6.

\end{thebibliography}


\end{document}
